version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: antigravity-uni-portal
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: always
